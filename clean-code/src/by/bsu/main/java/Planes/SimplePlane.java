package by.bsu.main.java.Planes;

public class SimplePlane extends Plane {
    public SimplePlane(String model, int maxSpeed, int maxFlightDistance, int maxLoadCapacity) {
        super(model, maxSpeed, maxFlightDistance, maxLoadCapacity);
    }
}
